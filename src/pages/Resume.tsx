import React from 'react';
import { TITLES } from '../constants/siteContent';
import Page from '../layout/Page';

const Resume = () => {
  return <Page title={TITLES.RESUME}></Page>;
};

export default Resume;
